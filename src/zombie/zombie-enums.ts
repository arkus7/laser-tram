export enum ZombieType {
  Normal = 'normal',
  Brainiac = 'brainiac',
  Zaba = 'zaba',
}

export enum ZombieState {
  Attack = 'attack',
  Dead = 'dead',
  Hurt = 'hurt',
  Run = 'run',
  Walk = 'walk',
  Jump = 'jump',
}
